name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Swift
        uses: swift-actions/setup-swift@v2
        with:
          swift-version: '6.2'

      - name: Install system dependencies
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get install -y libcurl4-openssl-dev libxml2-dev

      - name: Test (Linux)
        if: matrix.os == 'ubuntu-latest'
        # stop-commands prevents tests that emit ::error:: / ::notice:: strings
        # (e.g. GitHub Actions output formatting tests) from creating spurious
        # annotations in the check run UI.
        # --no-parallel: .enabled(if: CI == nil) guards are insufficient — parallel
        # execution still deadlocks on both Linux and macOS even with blocking tests
        # disabled. Tested without it in dac0f1f; both platforms hit the 30-min timeout.
        run: |
          echo "::stop-commands::test-fence"
          swift test --package-path AIDevToolsKit --no-parallel
          echo "::test-fence::"

      - name: Test (macOS)
        if: matrix.os == 'macos-latest'
        # stop-commands prevents tests that emit ::error:: / ::notice:: strings
        # (e.g. GitHub Actions output formatting tests) from creating spurious
        # annotations in the check run UI.
        # --no-parallel: .enabled(if: CI == nil) guards are insufficient — parallel
        # execution still deadlocks on both Linux and macOS even with blocking tests
        # disabled. Tested without it in dac0f1f; both platforms hit the 30-min timeout.
        run: |
          echo "::stop-commands::test-fence"
          swift test --package-path AIDevToolsKit --no-parallel
          echo "::test-fence::"
